# In test environment, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env.test                contains default values for the test environment
#  * .env.test.local          uncommitted local overrides for the test environment
#
# Real environment variables win over .env files.

DATABASE_URL="postgresql://tom@127.0.0.1:5432/centralink_test?serverVersion=16&charset=utf8"

MAILER_DSN=null://null
